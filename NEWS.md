# nlgeocoder 0.2

* Fixed the url for the PDOK geocoder (issue #7).

# nlgeocoder 0.1.4

* Restricted `addPdokTiles` minZoom to 6 (issue #4)
* `nl_geocoder` handling of non-results is improved: returns POINT EMPTY objects.
